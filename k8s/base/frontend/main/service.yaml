apiVersion: v1
kind: Service
metadata:
  name: main-frontend
  namespace: microservices
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: main-frontend
